{
  "name": "atomico-base_element-3",
  "description": "Try BaseEleemnt for React container",
  "version": "1.0.0",
  "source": "index.html",
  "scripts": {
    "build": "bundle index.html dist",
    "dev": "bundle index.html dist --watch --server",
    "prepare": "npm run build"
  },
  "dependencies": {
    "atomico": "^0.22.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "rxjs": "^6.5.4"
  },
  "devDependencies": {
    "bundle-cli": "^0.23.1"
  }
}
